
> require 'init'
> Variant = require 'variant'

> Ball = Variant('Ball')

> b = Ball { x = 2, y = 3 ,z = 4  }

> return b

> return tostring(b) 

> return getmetatable(b)

> return b:match({Ball = function() return "handling ball" end, That = function() return "wow" end})

> function Ball:test() return "testing " .. self.x end

> getmetatable(b).index = getmetatable(b)

> return b:test()

> return b
